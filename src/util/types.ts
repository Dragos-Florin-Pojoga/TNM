export interface RuntimeParams {
  speed: number;
  corridorWidth: number;
  wallHeight: number;
  seed: number;
  sensitivity: number;
  peakThreshold: number;
  minBeatSpacing: number;
  attackMs: number;
  releaseMs: number;
}
